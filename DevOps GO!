Дорожная карта DevOps (6–9 месяцев)
🔹 Месяц 1–2. Основы Linux + Git

Что учить:

Установка Linux (Ubuntu/Debian) на виртуалку.

Пользователи, группы, права (chmod, chown, sudo).

Работа с файлами (grep, awk, sed, find).

Сеть (ip a, ping, netstat, ss, ufw).

Управление сервисами (systemctl).

Git: clone, commit, push, branch.

Практика:

Подними на виртуалке nginx и сделай простую страничку.

Настрой пользователей и доступы.

Создай репозиторий на GitHub и загрузи туда конфиги.

🔹 Месяц 3. Bash + Автоматизация

Что учить:

Bash-скрипты (циклы, условия, переменные).

Планировщик заданий (cron).

PowerShell (базово).

Практика:

Скрипт, который делает бэкап базы PostgreSQL и кидает архив на другой сервер.

Скрипт, который мониторит сервис (nginx) и перезапускает, если он упал.

🔹 Месяц 4. Docker + Docker Compose

Что учить:

Docker: образы, контейнеры, сети, тома.

docker-compose.

Работа с DockerHub.

Практика:

Подними контейнер с PostgreSQL.

Подними веб-приложение (например, WordPress) в контейнере.

Используй docker-compose для связки “БД + приложение”.

🔹 Месяц 5. CI/CD (GitLab CI или GitHub Actions)

Что учить:

Что такое пайплайны.

Основы YAML.

GitHub Actions или GitLab CI.

Практика:

Сделай пайплайн, который при пуше в репозиторий билдит Docker-образ.

Настрой автодеплой сайта на сервер.

🔹 Месяц 6. Ansible + Terraform

Что учить:

Ansible: плейбуки, роли, inventory.

Terraform: базовое разворачивание серверов в облаке (Yandex Cloud / AWS / GCP).

Практика:

Ansible: разверни nginx на нескольких серверах одной командой.

Terraform: создай ВМ в облаке и задеплой туда Docker-приложение.

🔹 Месяц 7–8. Kubernetes (K8s)

Что учить:

Minikube/kind (локальный кластер).

kubectl.

Деплой приложения в Kubernetes.

Helm Charts.

Практика:

Разверни кластер minikube.

Задеплой туда свое приложение из Docker.

Настрой ingress (доступ по домену).

🔹 Месяц 9. Мониторинг и логирование

Что учить:

Prometheus + Grafana (метрики, алерты).

Loki или ELK (логи).

Практика:

Собери метрики с контейнеров и отобрази их в Grafana.

Сделай алерт “если nginx упал — прислать сообщение”.
